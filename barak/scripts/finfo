#!/usr/bin/env python
import sys
if len(sys.argv) < 2:
    sys.exit('usage: finfo [filenames] ')

import pyfits

for filename in sys.argv[1:]:
    f = pyfits.open(filename)
    f.info()
    f.close()
